@php
use App\Helpers\FoodImageHelper;
@endphp


<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Welcome - FoodHub</title>
    <link href="https://fonts.bunny.net/css2?family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #f9fafb;
            color: #111827;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 1200px;
            margin: 60px auto;
            padding: 0 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 40px;
            font-size: 2.5rem;
            color: #ef3b2d;
            letter-spacing: 1px;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
            gap: 25px;
        }

        .card {
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }

        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-content {
            padding: 20px;
        }

        .card-content h3 {
            margin: 0;
            font-size: 1.3rem;
            color: #111;
            font-weight: 700;
        }

        .price {
            color: #16a34a;
            font-weight: 700;
            font-size: 1.1rem;
            margin: 8px 0;
        }

        .card-content p {
            color: #555;
            font-size: 0.95rem;
            margin-bottom: 0;
        }

        .footer {
            text-align: center;
            margin-top: 60px;
            color: #777;
            font-size: 0.9rem;
        }

        .no-foods {
            text-align: center;
            color: #666;
            font-size: 1.1rem;
            margin-top: 40px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>🍽️ Latest Foods</h1>

        <div class="grid">
    @forelse($foods as $food)
        @php
            $imageUrl = \App\Helpers\FoodImageHelper::generateImage($food->name);
        @endphp

        <div class="card">
            <img 
                src="{{ $imageUrl }}" 
                alt="{{ $food->name }}" 
                onerror="this.src='https://picsum.photos/600/400?random={{ $loop->index }}'">
            <div class="card-content">
                <h3>{{ $food->name }}</h3>
                <p class="price">₦{{ number_format($food->price, 2) }}</p>
                <p>{{ Str::limit($food->description, 80) }}</p>
            </div>
        </div>
    @empty
        <p class="no-foods">No foods uploaded yet.</p>
    @endforelse
</div>



        <div class="footer">
            &copy; {{ date('Y') }} FoodHub | Fresh & Delicious Meals
        </div>
    </div>

</body>
</html>
